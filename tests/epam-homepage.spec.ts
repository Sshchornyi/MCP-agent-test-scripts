import { test, expect } from '@playwright/test';

test.describe('EPAM homepage - basic flows', () => {
  test('placeholder', async ({ page }) => {
    // TODO: full test will be added in next commit
  });
});
